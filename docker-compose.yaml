version: "3.9"

services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs-app
    ports:
      - "3000:3001"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: always
